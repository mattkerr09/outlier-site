{
  "_why": "Written by the app's ship pipeline so CI can compare the site's tier list to what the app actually ships. CI has no access to the app repo, and a gate that cannot see its reference used to be skipped there \u2014 which made its only enforcement a no-op. The version field is what stops this going stale silently: the gate refuses unless it matches the version the site advertises.",
  "cut_from_version": "1.11.839",
  "tier_display_order": [
    "nano",
    "lite",
    "quick",
    "compact",
    "plus",
    "vision38"
  ]
}
