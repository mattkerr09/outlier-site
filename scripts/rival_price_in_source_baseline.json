{
  "_why": "Ratchet. A price can fail to match for honest reasons: it is attributed to something other than a vendor page, or the source renders client-side. Baselined so only NEW breakage fails. EDIT BY REMOVAL ONLY -- adding an entry silences a real claim.",
  "vs/outlier-vs-cline/index.html": {
    "$6.80": "The page itself says this is user-reported in cline/cline issue #7558 and explicitly NOT a vendor statement. GitHub renders issue bodies client-side, so a curl of the cited URL cannot contain it. The claim is correctly attributed on the page; the gate cannot see the source."
  }
}
